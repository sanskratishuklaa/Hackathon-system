# HackNation — Required Environment Variables
# Copy this file to .env and fill in the real values.
# NEVER commit the real .env file to version control.

DB_URL=jdbc:mysql://localhost:3306/hacknation_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
DB_USERNAME=hacknation
DB_PASSWORD=YourStrongPasswordHere

# Generate with: openssl rand -base64 32
JWT_SECRET=ReplaceWithYourBase64Encoded256BitSecret
JWT_EXPIRATION=86400000
